#include <vector>
#include <fstream>
#include <iostream>
#include <chrono>
#include <string>
#include<memory>
namespace data
{
    class data
    {
    private:
        /* data */
    public:
        std::vector<std::vector<int>> plotScales;
        double energyScale;
        int squareScale;
    };

} // namespace data
